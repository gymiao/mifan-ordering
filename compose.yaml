services:
  ordering-app:
    build: .
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3000
    volumes:
      - order-data:/app/data
    ports:
      - "127.0.0.1:3000:3000"

volumes:
  order-data:
