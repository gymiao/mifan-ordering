{
  "name": "mifan-ordering",
  "version": "1.0.0",
  "private": true,
  "description": "Fast, dependency-free restaurant ordering MVP",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
